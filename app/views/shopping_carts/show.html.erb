<h1>My Shopping cart</h1>
<% @cart.purchases.each do |purchase| %>
    <div>Name <%= purchase.product.name %> </div>
    <div>Price <%= purchase.product.price %> </div>
    <div>Amount <%= purchase.amount %></div>
<% end %>


<%= button_to 'Create order', :create_order %>